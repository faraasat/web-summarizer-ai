{
  "name": "@repo/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "py-install": "python -m venv virt_env & source virt_env/bin/activate & pip install -r requirements.txt",
    "py-install-dev": "python -m venv virt_env & source virt_env/bin/activate & pip install -r requirements.txt -r requirements-dev.txt",
    "dev": "env/bin/python -m uvicorn summarizer_api.main:app --reload"
  }
}
